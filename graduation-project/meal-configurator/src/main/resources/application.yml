server:
  port: 8282

spring:
  application:
    name: meal-configurator
  messages:
    encoding: UTF-8
  data:
    mongodb:
      port: 2888
      database: day-meals
      host: localhost

eureka:
  client:
    service-url:
      defaultZone: http://localhost:9888/eureka/

logging:
  level:
    ROOT: ERROR
